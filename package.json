{
  "name": "simple_web_game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "upm run dev && firebase deploy"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^2.4.2",
    "svelte": "^4.0.3",
    "vite": "^4.4.0"
  },
  "dependencies": {
    "firebase": "^10.0.0"
  }
}
